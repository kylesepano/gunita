# Supabase SQL Editor seed files

The SQL Editor cannot run the complete seed at once. Open and run 01.sql, then 02.sql, and continue in numerical order through 09.sql. Each file is an independent transaction and can safely be re-run.
